# Video Feed

Drop 5 looping, portrait (9:16) MP4s here with these filenames to match the seeded feed:

- `sayram.mp4` — autumn lake / still water
- `pacific.mp4` — coastal wind / dusk horizon
- `dolomites.mp4` — misty mountains
- `mojave.mp4` — empty road at noon
- `tokyo.mp4` — city at night

Free sources: Pexels Videos, Pixabay, Coverr.co. Keep each file under ~8 MB for fast load.

If a file is missing the feed falls back to an animated gradient placeholder so the flow is still demoable.
